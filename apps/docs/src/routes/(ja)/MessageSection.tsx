import { component$ } from "@builder.io/qwik";

export const MessageSection = component$(() => {
  return (
    <section>
      <h2>
        アイデアを
        <br />
        形に
      </h2>
    </section>
  );
});
